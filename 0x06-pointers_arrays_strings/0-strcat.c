#include "main.h"
/**
 * _strcat - concatenates two strings
 * @dest: deatination string
 * @src: source string
 * Return: dest
 */
char *_strcat(char *dest, char *src)
{
	strcat(dest, src);
	return (dest);
}
